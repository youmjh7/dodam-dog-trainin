# GitHub Upload Rule

When the user asks to "깃허브 업로드해", "깃허브에 올려줘", "깃허브 업로드", "github push", or any similar upload request:
1. Automatically run the local batch script `.\push_to_github.bat` in the project root (`d:\강아지훈련`).
2. Synthesize the output and confirm whether the upload was successful to the user.
